name: auth-cloak

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true  # PostgreSQL has no route to/from internet or Docker host

volumes:
  postgres_data:

services:

  # ── PostgreSQL 16 ────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: auth-cloak-postgres
    restart: unless-stopped
    networks:
      - backend
    environment:
      POSTGRES_DB:       ${POSTGRES_DB}
      POSTGRES_USER:     ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    command: >
      postgres
        -c max_connections=100
        -c shared_buffers=256MB
        -c effective_cache_size=768MB
        -c maintenance_work_mem=64MB
        -c checkpoint_completion_target=0.9
        -c wal_buffers=16MB
        -c default_statistics_target=100
        -c log_min_duration_statement=1000
        -c log_checkpoints=on
        -c log_lock_waits=on
    security_opt:
      - no-new-privileges:true

  # ── Ory Kratos ───────────────────────────────────────────────────────────────
  kratos:
    image: oryd/kratos:v1.3
    container_name: auth-cloak-kratos
    restart: unless-stopped
    command: serve -c /etc/config/kratos/kratos.yml -c /etc/config/kratos/kratos.secrets.yml --dev=false
    networks:
      - frontend
      - backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable&max_conns=10&max_idle_conns=4
      KRATOS_SERVE_PUBLIC_BASE_URL:                        https://${KRATOS_HOSTNAME}/identity/
      KRATOS_SERVE_PUBLIC_CORS_ALLOWED_ORIGINS:            https://${FDS_HOSTNAME}
      KRATOS_SELFSERVICE_DEFAULT_BROWSER_RETURN_URL:       https://${FDS_HOSTNAME}/
      KRATOS_SELFSERVICE_FLOWS_LOGIN_UI_URL:               https://${FDS_HOSTNAME}/login
      KRATOS_SELFSERVICE_FLOWS_REGISTRATION_UI_URL:        https://${FDS_HOSTNAME}/register
      KRATOS_SELFSERVICE_FLOWS_SETTINGS_UI_URL:            https://${FDS_HOSTNAME}/settings
      KRATOS_SELFSERVICE_FLOWS_RECOVERY_UI_URL:            https://${FDS_HOSTNAME}/recovery
      KRATOS_SELFSERVICE_FLOWS_VERIFICATION_UI_URL:        https://${FDS_HOSTNAME}/verification
    volumes:
      - ./kratos/config:/etc/config/kratos:ro
    ports:
      - "${KRATOS_ADMIN_BIND_IP}:4433:4433"  # Public API — nginx proxies externally; FDS API calls directly over LAN
    ports:
      # Admin API — bound to LAN IP so FDS API (Server 2) can reach it over LAN.
      # Never proxied through Nginx. Firewall restricts to FDS server LAN IP only.
      - "${KRATOS_ADMIN_BIND_IP}:4434:4434"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:4433/health/ready || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    security_opt:
      - no-new-privileges:true

  # ── Nginx ────────────────────────────────────────────────────────────────────
  nginx:
    image: nginx:1.27-alpine
    container_name: auth-cloak-nginx
    restart: unless-stopped
    networks:
      - frontend
    depends_on:
      kratos:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    environment:
      SSL_CERT_DIR: ${SSL_CERT_DIR}
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/templates:/etc/nginx/templates:ro
      - /dev/null:/etc/nginx/conf.d/default.conf:ro
      # Mount full letsencrypt tree so symlinks in live/ resolve to archive/
      - /etc/letsencrypt:/etc/letsencrypt:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    security_opt:
      - no-new-privileges:true
